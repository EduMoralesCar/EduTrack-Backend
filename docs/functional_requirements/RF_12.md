# RF 12: Consulta de rendimiento académico

- **Explicación del Requerimiento:**
  Este requerimiento permite a los estudiantes inscritos en el sistema EduTrack consultar de manera centralizada su rendimiento académico actual a través del panel principal (Dashboard). La información desplegada comprende:
  1. **Promedio General (GPA):** La nota promedio ponderada a través de todos los cursos matriculados del alumno en el semestre.
  2. **Asistencia Promedio:** El porcentaje general de asistencia, calculado a partir de la relación entre sesiones asistidas (con estado `PRESENTE`, `TARDE` o `JUSTIFICADO`) y el total de sesiones registradas.
  3. **Estado Académico General:** Muestra de manera visual y descriptiva el estado consolidado de la matrícula académica del alumno. Los estados visuales son:
     - `ACTIVO` (Cuando el estudiante está en estado regular en todos sus cursos)
     - `OBSERVADO` (Si el estudiante figura en estado `AT_RISK` en al menos una de sus secciones matriculadas)
     - `RETIRADO` (Si el estudiante se retiró de sus cursos o ha sido retirado administrativamente)

- **Flujo de Código Paso a Paso:**

  1. **Interfaz de Usuario (Frontend):**
     - **Archivo de componente:** [Dashboard.jsx](file:///d:/Workspace/utp/EduTrack/Frontend/src/components/Dashboard.jsx)
     - **Acción del usuario:** Al iniciar sesión exitosamente en EduTrack con credenciales de alumno, el panel se inicializa y ejecuta la función `loadStudentSummary` dentro de un hook `useEffect` al cargarse el rol `STUDENT`.
     - **Código exacto de fetch de Javascript:**
       En [Dashboard.jsx](file:///d:/Workspace/utp/EduTrack/Frontend/src/components/Dashboard.jsx) (líneas 119-162):
       ```javascript
       const myEnrollments = await api.enrollments.getMyEnrollments();
       
       let totalGradesSum = 0;
       let gradedSectionsCount = 0;
       let totalPresent = 0;
       let totalSessions = 0;
       let isAtRisk = false;
       let activeStatus = 'ENROLLED';

       for (const enr of myEnrollments) {
         if (enr.status === 'AT_RISK') {
           isAtRisk = true;
         }
         if (enr.status === 'WITHDRAWN' && activeStatus !== 'AT_RISK') {
           activeStatus = 'WITHDRAWN';
         }
         
         // Grades
         try {
           const finalGrades = await api.grades.getFinalGrades(enr.sectionId);
           if (finalGrades && finalGrades.length > 0) {
             const myGrade = finalGrades[0];
             if (myGrade.finalAverage != null) {
               totalGradesSum += myGrade.finalAverage;
               gradedSectionsCount++;
             }
           }
         } catch (e) {
           console.error("Error loading final grade for section " + enr.sectionId, e);
         }

         // Attendance
         try {
           const sectionAttendance = await api.attendance.getBySection(enr.sectionId);
           if (sectionAttendance && sectionAttendance.length > 0) {
             totalSessions += sectionAttendance.length;
             const presentCount = sectionAttendance.filter(a => 
               a.status === 'PRESENTE' || a.status === 'TARDE' || a.status === 'JUSTIFICADO'
             ).length;
             totalPresent += presentCount;
           }
         } catch (e) {
           console.error("Error loading attendance for section " + enr.sectionId, e);
         }
       }
       ```

  2. **Capa de Red:**
     - **HTTP Method:** Múltiples solicitudes concurrentes de tipo `GET`.
     - **Base URL:** `http://localhost:8081/api`
     - **Paths de red llamados:**
       - `/enrollments/my` (para listar las secciones del alumno)
       - `/grades/section/{sectionId}/final` (para obtener las notas consolidadas de la sección)
       - `/attendance/section/{sectionId}` (para obtener el histórico de asistencia en la sección)
     - **Request parameters:** `sectionId` en los endpoints de notas y asistencia de cada sección.
     - **Request body format:** Vacío.
     - **Cabeceras:** `Authorization: Bearer <token>` con el JWT token de sesión.

  3. **Controlador (Backend Controller):**
     - **Clases Controller implicadas:** 
       - [EnrollmentController](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/web/controller/EnrollmentController.java) (para `/enrollments/my`)
       - [GradeController](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/web/controller/GradeController.java) (para `/grades/section/{sectionId}/final`)
       - [AttendanceController](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/web/controller/AttendanceController.java) (para `/attendance/section/{sectionId}`)
     - **Métodos del endpoint:** `findMyEnrollments`, `getSectionFinalGrades`, `getSectionAttendance`.
     - **Snippet de código Java (Ejemplo de Matrículas):**
       ```java
       @GetMapping("/my")
       @PreAuthorize("hasAnyRole('STUDENT','ADMIN')")
       @Operation(summary = "Listar mis matrículas (STUDENT)", description = "Permite al estudiante ver las secciones donde está matriculado")
       public ResponseEntity<List<EnrollmentDTO>> findMyEnrollments() {
           UserPrincipal principal = authService.getCurrentPrincipal();
           return ResponseEntity.ok(enrollmentService.findByStudent(principal.getId()));
       }
       ```

  4. **Capa de Negocio (Backend Service):**
     - **Clase Service:** [EnrollmentService](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/domain/service/EnrollmentService.java)
     - **Método de implementación:** `findByStudent`
     - **DTO de mapeo:** [EnrollmentDTO](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/domain/dto/EnrollmentDTO.java)
     - **Validaciones:** Se obtiene el identificador de usuario `studentId` decodificado de manera segura desde la sesión JWT del contexto de seguridad de Spring (evitando suplantaciones).
     - **Snippet de código Java:**
       ```java
       @Transactional(readOnly = true)
       public List<EnrollmentDTO> findByStudent(Long studentId) {
           return enrollmentRepository.findByStudentId(studentId).stream()
                   .map(enrollmentMapper::toDto)
                   .toList();
       }
       ```

  5. **Capa de Persistencia (Repository & Entity):**
     - **Repository:** [EnrollmentRepository](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/persistance/repository/EnrollmentRepository.java)
     - **Spring Data JPA Method:** `List<Enrollment> findByStudentId(Long studentId);`
     - **JPA Entity:** [Enrollment](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/persistance/entity/Enrollment.java) (tabla `enrollments`), [User](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/persistance/entity/User.java) (tabla `users`).
     - **Funcionamiento de Base de Datos:**
       La consulta mapea una relación directa a nivel de base de datos a través de una sentencia `SELECT * FROM enrollments e WHERE e.student_id = ?` cargando el objeto matrícula, el identificador de sección y el estado académico actual (`ENROLLED`, `WITHDRAWN`, `AT_RISK`).

  6. **Camino de Retorno:**
     - El backend retorna los datos consultados como DTOs en formato JSON de forma asíncrona hacia el frontend.
     - En el frontend ([Dashboard.jsx](file:///d:/Workspace/utp/EduTrack/Frontend/src/components/Dashboard.jsx)), las promesas de fetch se resuelven de forma consecutiva dentro del bucle de matrículas. Una vez consolidadas las notas de todas las asignaturas y calculados los totales de asistencias, se computa en el estado React el resumen del estudiante:
       ```javascript
       setStudentSummary({
         gpa: gpa != null ? Math.round(gpa * 100) / 100 : null,
         attendanceRate: attendanceRate != null ? Math.round(attendanceRate * 10000) / 100 : null,
         status: activeStatus,
         loading: false
       });
       ```
       Esto actualiza el árbol DOM y redibuja las tres tarjetas de rendimiento superior con gradientes y microanimaciones de alerta (rojo para promedios menores a 12.5 o asistencias menores al 70.0%).
