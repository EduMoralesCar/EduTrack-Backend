# RF 10: Justificación de inasistencias

- **Explicación del Requerimiento**:
  Este requerimiento permite a los estudiantes justificar sus inasistencias (`AUSENTE`) registradas en el sistema. El estudiante puede enviar una solicitud describiendo el motivo e incluyendo un archivo digital de sustento (por ejemplo, un certificado médico en formato PDF o imagen). Las solicitudes se registran en estado `PENDIENTE`. Los docentes (`TEACHER`) y administradores (`ADMIN`) pueden revisar la lista de justificaciones recibidas para su sección y decidir aprobarlas (`APROBADO`) o rechazarlas (`RECHAZADO`). Si se aprueba, el estado de la asistencia cambia automáticamente a `JUSTIFICADO` (lo que no penaliza la tasa de asistencia del alumno); si se rechaza, permanece como `AUSENTE`. El sistema notifica al estudiante sobre la resolución tomada.

- **Flujo de Código Paso a Paso**:

  1. **Interfaz de Usuario (Frontend)**:
     - **Componente React**: [Attendance.jsx](file:///d:/Workspace/utp/EduTrack/Frontend/src/components/Attendance.jsx)
     - **Acciones del Usuario**:
       - **Estudiante (Envío)**: Si tiene un registro de asistencia en estado "Ausente", se le habilita el botón "Justificar Falta". Al dar clic, se despliega un modal donde describe el motivo, adjunta el archivo de prueba y presiona "Enviar", desencadenando `handleSubmitJustification`.
       - **Docente (Resolución)**: Navega a la pestaña de justificaciones de la sección. Examina el sustento y da clic en el botón "Aprobar" o "Rechazar", lo que dispara la función `handleResolveJustification`.
     - **Código de JavaScript (Fetch)**:
       ```javascript
       // In d:/Workspace/utp/EduTrack/Frontend/src/components/Attendance.jsx
       
       // Solicitud del Estudiante
       const handleSubmitJustification = async (e) => {
         e.preventDefault();
         if (!submittingJustification) return;
       
         const attendanceId = submittingJustification.attendanceId;
         const reason = e.target.elements.reason.value;
         const proofFile = e.target.elements.proofFile.files[0];
       
         if (!reason) {
           showNotification('Por favor ingrese el motivo.', 'danger');
           return;
         }
       
         try {
           await api.attendance.submitJustification(attendanceId, reason, proofFile);
           showNotification('Solicitud de justificación enviada correctamente.');
           setSubmittingJustification(null);
           await loadSectionData(selectedSection); // Recarga asistencia
         } catch (err) {
           showNotification('Error al enviar justificación: ' + err.message, 'danger');
         }
       };

       // Resolución del Docente
       const handleResolveJustification = async (justificationId, status) => {
         try {
           await api.attendance.resolveJustification(justificationId, status); // status: 'APROBADO' o 'RECHAZADO'
           showNotification(`Solicitud de justificación ${status === 'APROBADO' ? 'aprobada' : 'rechazada'} correctamente.`);
           
           // Recarga listas locales
           await loadJustifications(selectedSection);
           await loadSectionData(selectedSection);
         } catch (err) {
           showNotification('Error al resolver justificación: ' + err.message, 'danger');
         }
       };

       // In d:/Workspace/utp/EduTrack/Frontend/src/services/api.js
       attendance: {
         submitJustification: (attendanceId, reason, proofFile) => {
           const formData = new FormData();
           formData.append('reason', reason);
           if (proofFile) {
             formData.append('proofFile', proofFile);
           }
           return request(`/attendance/${attendanceId}/justify`, {
             method: 'POST',
             body: formData,
           });
         },
         resolveJustification: (justificationId, status) => request(`/attendance/justifications/${justificationId}/resolve?status=${status}`, {
           method: 'PUT',
         }),
         // ...
       }
       ```

  2. **Capa de Red**:
     - **Para el Envío**:
       - **Método HTTP**: `POST`
       - **Ruta (Path)**: `/attendance/{attendanceId}/justify`
       - **Cuerpo de Solicitud (Request Body)**: Codificado como `multipart/form-data` con los parámetros `reason` (String) y `proofFile` (Binary).
     - **Para la Resolución**:
       - **Método HTTP**: `PUT`
       - **Ruta (Path)**: `/attendance/justifications/{justificationId}/resolve`
       - **Parámetros de Solicitud**: `status` (Query Param: `APROBADO` o `RECHAZADO`).
       - **Cuerpo de Solicitud**: Vacío.

  3. **Controlador (Backend Controller)**:
     - **Nombre de Clase**: [AttendanceController.java](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/web/controller/AttendanceController.java)
     - **Métodos del Endpoint**: `submitJustification` y `resolveJustification`
     - **Snippet de Código Java**:
       ```java
       @PostMapping("/{attendanceId}/justify")
       @PreAuthorize("hasRole('STUDENT')")
       @Operation(summary = "Solicitar justificación (RF10)", description = "Permite a un estudiante justificar una falta, con archivo de evidencia opcional. Accesible solo por ESTUDIANTE.")
       public ResponseEntity<AttendanceJustificationDTO> submitJustification(
               @PathVariable Long attendanceId,
               @RequestParam("reason") String reason,
               @RequestParam(value = "proofFile", required = false) MultipartFile proofFile) {
           return new ResponseEntity<>(attendanceService.submitJustification(attendanceId, reason, proofFile), HttpStatus.CREATED);
       }

       @PutMapping("/justifications/{justificationId}/resolve")
       @PreAuthorize("hasAnyRole('TEACHER', 'ADMIN')")
       @Operation(summary = "Resolver justificación", description = "Permite a un docente o administrador aprobar o rechazar una justificación de inasistencia.")
       public ResponseEntity<Void> resolveJustification(
               @PathVariable Long justificationId,
               @RequestParam("status") JustificationStatus status) {
           attendanceService.resolveJustification(justificationId, status);
           return ResponseEntity.ok().build();
       }
       ```

  4. **Capa de Negocio (Backend Service)**:
     - **Nombre de Clase**: [AttendanceService.java](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/domain/service/AttendanceService.java)
     - **Implementación de los Métodos**:
       - `submitJustification`: Registra la justificación en BD en estado `PENDIENTE` y asocia el archivo subido en el file server mediante `fileStorageService`.
       - `resolveJustification`: Cambia el estado de la justificación a `APROBADO` o `RECHAZADO`. Al aprobar, cambia además el estado del registro de asistencia relacionado a `JUSTIFICADO` (si se rechaza, retorna a `AUSENTE`). Re-evalúa el riesgo académico e informa la decisión al estudiante.
     - **Snippet de Código Java**:
       ```java
       @Transactional
       public AttendanceJustificationDTO submitJustification(Long attendanceId, String reason, MultipartFile proofFile) {
           Attendance attendance = attendanceRepository.findById(attendanceId)
                   .orElseThrow(() -> new ResourceNotFoundException("Attendance not found"));

           String proofFilePath = null;
           if (proofFile != null && !proofFile.isEmpty()) {
               proofFilePath = fileStorageService.storeFile(proofFile);
           }

           AttendanceJustification justification = AttendanceJustification.builder()
                   .attendance(attendance)
                   .reason(reason)
                   .proofFilePath(proofFilePath)
                   .status(JustificationStatus.PENDIENTE)
                   .submittedAt(LocalDateTime.now())
                   .build();

           AttendanceJustification saved = justificationRepository.save(justification);

           return AttendanceJustificationDTO.builder()
                   .id(saved.getId())
                   .attendanceId(saved.getAttendance().getId())
                   .reason(saved.getReason())
                   .proofFilePath(saved.getProofFilePath())
                   .status(saved.getStatus())
                   .submittedAt(saved.getSubmittedAt())
                   .build();
       }

       @Transactional
       public void resolveJustification(Long justificationId, JustificationStatus status) {
           AttendanceJustification justification = justificationRepository.findById(justificationId)
                   .orElseThrow(() -> new ResourceNotFoundException("Justification not found"));
           
           justification.setStatus(status);
           justificationRepository.save(justification);

           Attendance attendance = justification.getAttendance();
           if (status == JustificationStatus.APROBADO) {
               attendance.setStatus(com.utp.EduTrack.persistance.entity.AttendanceStatus.JUSTIFICADO);
               attendanceRepository.save(attendance);
           } else if (status == JustificationStatus.RECHAZADO) {
               attendance.setStatus(com.utp.EduTrack.persistance.entity.AttendanceStatus.AUSENTE);
               attendanceRepository.save(attendance);
           }

           try {
               enrollmentService.evaluateAcademicRisk(attendance.getStudent().getId(), attendance.getSection().getId());
               
               String statusString = (status == JustificationStatus.APROBADO) ? "APROBADA" : "RECHAZADA";
               notificationService.sendNotification(attendance.getStudent(), 
                       "Tu solicitud de justificación para la fecha " + attendance.getDate() + 
                       " en la sección " + attendance.getSection().getCode() + " ha sido " + statusString + ".");
           } catch (Exception e) {
               System.err.println("Warning evaluating risk or sending justification resolution notification: " + e.getMessage());
           }
       }
       ```

  5. **Capa de Persistencia (Repository & Entity)**:
     - **Repositorio**: [AttendanceJustificationRepository.java](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/persistance/repository/AttendanceJustificationRepository.java)
       - **Métodos JPA Utilizados**: `save(AttendanceJustification justification)` para insertar la justificación o actualizar su estado, y `findByAttendanceIdIn(...)` o `findByAttendanceSectionId(...)` para listar solicitudes.
     - **Entidad**: [AttendanceJustification.java](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/persistance/entity/AttendanceJustification.java) y [Attendance.java](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/persistance/entity/Attendance.java)
       - **Tablas de Base de Datos**: `attendance_justifications` y `attendances`
       - **Detalles de Registro/Actualización/Consulta**: 
         - Al justificar, se hace un `INSERT` en `attendance_justifications` asociando la FK `attendance_id` y asignando la fecha de envío y la ruta física de la evidencia.
         - Al resolver, se ejecuta un `UPDATE` en la tabla `attendance_justifications` para cambiar `status` (`APROBADO` o `RECHAZADO`), seguido por un `UPDATE` en la tabla `attendances` sobre el registro asociado para cambiar su `status` a `JUSTIFICADO` (en caso de aprobación) o mantenerlo en `AUSENTE`.

  6. **Camino de Retorno**:
     - **Envío**: El backend retorna un HTTP `201 Created` con los datos de la justificación. El frontend en `Attendance.jsx` recibe esto en `handleSubmitJustification`, muestra una alerta de éxito, establece `setSubmittingJustification(null)` para cerrar el modal y ejecuta `loadSectionData(...)` para recargar el listado y mostrar el badge como "Pendiente" de revisión.
     - **Resolución**: El backend procesa los cambios de estado en cascada de ambas tablas y retorna un HTTP `200 OK` vacío. El componente en el frontend captura la respuesta exitosa en `handleResolveJustification`, dispara `showNotification` con el aviso correspondiente e invoca `loadJustifications(...)` y `loadSectionData(...)` en paralelo. Esto re-renderiza la grilla y el listado de justificaciones del docente, actualizando el estado de asistencia del estudiante a "Justificado" e incrementando su tasa general de asistencia.
