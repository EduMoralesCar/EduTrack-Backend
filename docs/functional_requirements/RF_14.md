# RF 14: Detección de riesgo académico

- **Explicación del Requerimiento:**
  Este requerimiento define las reglas automáticas de detección de riesgo académico del sistema EduTrack. Cada vez que se registra o actualiza una nota de un estudiante (RF08), el sistema desencadena de manera asíncrona o transaccional un proceso de auditoría y evaluación del estado académico en base a dos factores primordiales:
  1. **Rendimiento Académico Insuficiente:** Si el promedio final ponderado acumulado en la asignatura del estudiante es **menor a 11.5** (nota mínima aprobatoria en el modelo de la UTP).
  2. **Inasistencias Críticas:** Si la tasa de asistencia acumulada del estudiante (asistencias de tipo `PRESENTE`, `TARDE` o `JUSTIFICADO` sobre el total de sesiones controladas) se reduce **por debajo del 70%** (0.70).

  Si el estudiante incurre en cualquiera de estas condiciones, el sistema actualiza automáticamente el estado de su matrícula a `AT_RISK` (Observado) y le envía una notificación explicando detalladamente el motivo de su observación (bajo promedio, faltas, o ambos).
  Si el alumno logra subir su promedio por encima de 11.5 y/o recuperar su tasa de asistencia gracias a justificaciones, el sistema restablece de forma automática su estado a `ENROLLED` (Activo) y le notifica para motivar su avance.

- **Flujo de Código Paso a Paso:**

  1. **Interfaz de Usuario (Frontend):**
     - **Archivo de componente:** [Grades.jsx](file:///d:/Workspace/utp/EduTrack/Frontend/src/components/Grades.jsx)
     - **Acción del usuario:** Un docente digita una nueva nota para el estudiante y presiona el botón Guardar en el cronograma de 18 semanas, invocando la función `handleGradeSubmit`.
     - **Código exacto de fetch de Javascript:**
       En [Grades.jsx](file:///d:/Workspace/utp/EduTrack/Frontend/src/components/Grades.jsx) (líneas 140-158):
       ```javascript
       const handleGradeSubmit = async (e, assignmentId, studentId) => {
         e.preventDefault();
         const score = e.target.elements.score.value;
         try {
           await api.grades.record({
             assignmentId,
             studentId,
             score: parseFloat(score),
             teacherComment: 'Registrado desde UI de Calificaciones'
           });
           showNotification('Calificación registrada correctamente.');
           const finals = await api.grades.getFinalGrades(selectedSection);
           setFinalGrades(user.role === 'STUDENT' ? finals.filter(fg => fg.studentUsername === user.username) : finals);
           const inds = await api.grades.getSectionGrades(selectedSection);
           setIndividualGrades(inds);
         } catch (err) {
           showNotification('Error al registrar nota: ' + err.message, 'danger');
         }
       };
       ```

  2. **Capa de Red:**
     - **HTTP Method:** `POST`
     - **Base URL:** `http://localhost:8081/api`
     - **Path:** `/grades`
     - **Request parameters:** Ninguno.
     - **Request body format (JSON):**
       ```json
       {
         "assignmentId": 12,
         "studentId": 3,
         "score": 10.0,
         "teacherComment": "Registrado desde UI de Calificaciones"
       }
       ```

  3. **Controlador (Backend Controller):**
     - **Clase Controller:** [GradeController](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/web/controller/GradeController.java)
     - **Método del endpoint:** `recordGrade`
     - **Snippet de código Java:**
       ```java
       @PostMapping
       @PreAuthorize("hasAnyRole('TEACHER', 'ADMIN')")
       @Operation(summary = "Registrar nota (RF08)", description = "Registra una calificación para la entrega de un estudiante. La nota debe ser entre 1 y 20. Accesible por DOCENTE y ADMIN.")
       public ResponseEntity<GradeDTO> recordGrade(@RequestBody GradeDTO dto) {
           return new ResponseEntity<>(gradeService.recordGrade(dto), HttpStatus.CREATED);
       }
       ```

  4. **Capa de Negocio (Backend Service):**
     - **Clase Service:** [GradeService](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/domain/service/GradeService.java) y [EnrollmentService](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/domain/service/EnrollmentService.java)
     - **Método de implementación:** `evaluateAcademicRisk` (invocado internamente desde el método `recordGrade` de `GradeService`).
     - **DTO de mapeo:** [GradeDTO](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/domain/dto/GradeDTO.java)
     - **Validaciones:** Se comprueba que la nota esté en el rango de `0` y `20`. Después de registrar la calificación, se gatilla `evaluateAcademicRisk(studentId, sectionId)`. Si la matrícula no figura como retirada (`WITHDRAWN`), se verifica el promedio de notas finales y el porcentaje acumulado de asistencias.
     - **Snippet de código Java:**
       ```java
       public void evaluateAcademicRisk(Long studentId, Long sectionId) {
           Enrollment enrollment = enrollmentRepository.findByStudentIdAndSectionId(studentId, sectionId)
                   .orElse(null);
           if (enrollment == null) {
               return;
           }

           if (enrollment.getStatus() == EnrollmentStatus.WITHDRAWN) {
               return;
           }

           boolean isLowGrades = false;
           boolean isLowAttendance = false;

           // 1. Check Grades
           List<com.utp.EduTrack.domain.dto.FinalGradeDTO> finalGrades = gradeService.getSectionFinalGrades(sectionId);
           com.utp.EduTrack.domain.dto.FinalGradeDTO studentFinalGrade = finalGrades.stream()
                   .filter(fg -> fg.getStudentId().equals(studentId))
                   .findFirst()
                   .orElse(null);

           if (studentFinalGrade != null && studentFinalGrade.getFinalAverage() != null) {
               if (studentFinalGrade.getFinalAverage() < 11.5) {
                   isLowGrades = true;
               }
           }

           // 2. Check Attendance
           List<com.utp.EduTrack.persistance.entity.Attendance> attendances = attendanceRepository.findBySectionIdAndStudentId(sectionId, studentId);
           if (!attendances.isEmpty()) {
               long totalSessions = attendances.size();
               long presentSessions = attendances.stream()
                       .filter(a -> a.getStatus() == com.utp.EduTrack.persistance.entity.AttendanceStatus.PRESENTE 
                                 || a.getStatus() == com.utp.EduTrack.persistance.entity.AttendanceStatus.TARDE
                                 || a.getStatus() == com.utp.EduTrack.persistance.entity.AttendanceStatus.JUSTIFICADO)
                       .count();
               double attendanceRate = (double) presentSessions / totalSessions;
               if (attendanceRate < 0.70) {
                   isLowAttendance = true;
               }
           }

           EnrollmentStatus oldStatus = enrollment.getStatus();
           EnrollmentStatus newStatus = (isLowGrades || isLowAttendance) ? EnrollmentStatus.AT_RISK : EnrollmentStatus.ENROLLED;

           if (oldStatus != newStatus) {
               enrollment.setStatus(newStatus);
               enrollmentRepository.save(enrollment);

               if (newStatus == EnrollmentStatus.AT_RISK) {
                   String message = String.format("Alerta de Riesgo Académico: Has sido marcado con estado 'En Riesgo/Observado' en la sección %s debido a: %s.",
                           enrollment.getSection().getCode(),
                           (isLowGrades && isLowAttendance) ? "bajo rendimiento y faltas recurrentes" :
                                   (isLowGrades ? "bajo rendimiento académico" : "inasistencias recurrentes"));
                   notificationService.sendNotification(enrollment.getStudent(), message);
               } else if (oldStatus == EnrollmentStatus.AT_RISK && newStatus == EnrollmentStatus.ENROLLED) {
                   String message = String.format("Actualización Académica: Tu estado en la sección %s ha vuelto a 'Activo'. ¡Buen trabajo!",
                           enrollment.getSection().getCode());
                   notificationService.sendNotification(enrollment.getStudent(), message);
               }
           }
       }
       ```

  5. **Capa de Persistencia (Repository & Entity):**
     - **Repository:** 
       - [EnrollmentRepository](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/persistance/repository/EnrollmentRepository.java) -> `Optional<Enrollment> findByStudentIdAndSectionId(Long studentId, Long sectionId);`
       - [AttendanceRepository](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/persistance/repository/AttendanceRepository.java) -> `List<Attendance> findBySectionIdAndStudentId(Long sectionId, Long studentId);`
     - **JPA Entities:**
       - [Enrollment](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/persistance/entity/Enrollment.java) (tabla `enrollments`)
       - [Attendance](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/persistance/entity/Attendance.java) (tabla `attendances`)
       - [Grade](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/persistance/entity/Grade.java) (tabla `grades`)
     - **Funcionamiento de Base de Datos:**
       Si la validación de negocio determina un cambio de riesgo, Hibernate ejecuta una sentencia `UPDATE enrollments SET status = 'AT_RISK' WHERE id = ?` (o 'ENROLLED' si se recupera), además de un `INSERT INTO notifications` para registrar la alerta correspondiente.

  6. **Camino de Retorno:**
     - El `GradeService` retorna el `GradeDTO` de la calificación registrada al controlador con HTTP 201 Created.
     - El cliente frontend recibe la respuesta de creación de la nota exitosa, repinta las notas registradas y efectúa la consulta del consolidado final (`getFinalGrades`) y asistencias para refrescar las vistas del docente y, en caso del alumno, sus paneles generales en tiempo real.
