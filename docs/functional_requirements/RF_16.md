# RF 16: Notificaciones académicas

- **Explicación del Requerimiento:**
  Proporciona a los estudiantes un canal de comunicación directo en tiempo real para recibir alertas académicas preventivas e informativas. Las notificaciones se disparan automáticamente en los siguientes escenarios:
  - Registro o actualización de una nota de evaluación (PC1, PC2, PC3, PA, EXFINAL).
  - Detección automática de riesgo académico (cuando su estado de matrícula pasa a ser Observado / `AT_RISK`).
  - Resolución automática de salida de riesgo (cuando su matrícula vuelve a estar Activa / `ENROLLED`).
  - Cambios manuales de estado de matrícula realizados por el administrador o docente.

  El estudiante puede visualizar las alertas mediante un indicador numérico flotante en forma de campana en el panel de control y puede marcar notificaciones individuales o masivamente como leídas.

- **Flujo de Código Paso a Paso:**

  1. **Interfaz de Usuario (Frontend):**
     - **Archivo de componente:** [Dashboard.jsx](file:///d:/Workspace/utp/EduTrack/Frontend/src/components/Dashboard.jsx) (subcomponente interno `NotificationTray`).
     - **Acción del usuario:** El subcomponente se monta en la barra de cabecera y configura un sondeo automático (polling) de peticiones fetch cada 20 segundos llamando a `fetchNotifications()`. El alumno también puede hacer clic en la campana para ver sus alertas, o presionar una alerta o "Marcar todas" para registrarlas como leídas.
     - **Código exacto de fetch de Javascript:**
       En [Dashboard.jsx](file:///d:/Workspace/utp/EduTrack/Frontend/src/components/Dashboard.jsx) (líneas 920-959):
       ```javascript
       function NotificationTray() {
         const [notifications, setNotifications] = useState([]);
         const [unreadCount, setUnreadCount] = useState(0);
         const [isOpen, setIsOpen] = useState(false);

         const fetchNotifications = async () => {
           try {
             const data = await api.notifications.getAll();
             setNotifications(data);
             const countData = await api.notifications.getUnreadCount();
             setUnreadCount(countData.unreadCount);
           } catch (e) {
             console.error("Error fetching notifications:", e);
           }
         };

         useEffect(() => {
           fetchNotifications();
           const interval = setInterval(fetchNotifications, 20000);
           return () => clearInterval(interval);
         }, []);

         const handleMarkAllAsRead = async () => {
           try {
             await api.notifications.markAllAsRead();
             fetchNotifications();
           } catch (e) {
             console.error(e);
           }
         };

         const handleMarkAsRead = async (id) => {
           try {
             await api.notifications.markAsRead(id);
             fetchNotifications();
           } catch (e) {
             console.error(e);
           }
         };
         
         // ...
       }
       ```
       En [api.js](file:///d:/Workspace/utp/EduTrack/Frontend/src/services/api.js):
       ```javascript
       notifications: {
         getAll: () => request('/notifications'),
         getUnreadCount: () => request('/notifications/unread-count'),
         markAsRead: (id) => request(`/notifications/${id}/read`, { method: 'PUT' }),
         markAllAsRead: () => request('/notifications/read-all', { method: 'PUT' }),
       }
       ```

  2. **Capa de Red:**
     - **HTTP Method:** `GET` (listar y contar) / `PUT` (marcar lectura).
     - **Base URL:** `http://localhost:8081/api`
     - **Paths:**
       - `/notifications` (Listado de notificaciones)
       - `/notifications/unread-count` (Obtener conteo de no leídas)
       - `/notifications/{id}/read` (Lectura individual)
       - `/notifications/read-all` (Lectura masiva de todas)
     - **Request parameters:** `id` en la ruta como `@PathVariable` para lectura individual.
     - **Request body format:** Vacío.
     - **Cabeceras obligatorias:** `Authorization: Bearer <token>` (el rol debe ser `STUDENT`).

  3. **Controlador (Backend Controller):**
     - **Clase Controller:** [NotificationController](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/web/controller/NotificationController.java)
     - **Métodos del endpoint:** `getMyNotifications`, `getMyUnreadCount`, `markAsRead`, `markAllAsRead`.
     - **Snippet de código Java:**
       ```java
       @RestController
       @RequestMapping("/api/notifications")
       @RequiredArgsConstructor
       @SecurityRequirement(name = OpenApiConfig.BEARER_AUTH)
       @Tag(name = "Notificaciones (ESTUDIANTE)", description = "RF16 - Alertas académicas automáticas para el estudiante")
       public class NotificationController {

           private final NotificationService notificationService;
           private final AuthService authService;

           @GetMapping
           @PreAuthorize("hasRole('STUDENT')")
           @Operation(summary = "Obtener notificaciones del alumno", description = "Lista las notificaciones del alumno logueado ordenadas por fecha")
           public ResponseEntity<List<NotificationDTO>> getMyNotifications() {
               UserPrincipal principal = authService.getCurrentPrincipal();
               return ResponseEntity.ok(notificationService.getNotificationsForStudent(principal.getId()));
           }

           @GetMapping("/unread-count")
           @PreAuthorize("hasRole('STUDENT')")
           @Operation(summary = "Obtener cantidad de notificaciones no leídas")
           public ResponseEntity<Map<String, Long>> getMyUnreadCount() {
               UserPrincipal principal = authService.getCurrentPrincipal();
               Map<String, Long> response = new HashMap<>();
               response.put("unreadCount", notificationService.getUnreadCount(principal.getId()));
               return ResponseEntity.ok(response);
           }

           @PutMapping("/{id}/read")
           @PreAuthorize("hasRole('STUDENT')")
           @Operation(summary = "Marcar notificación como leída")
           public ResponseEntity<Void> markAsRead(@PathVariable Long id) {
               notificationService.markAsRead(id);
               return ResponseEntity.noContent().build();
           }

           @PutMapping("/read-all")
           @PreAuthorize("hasRole('STUDENT')")
           @Operation(summary = "Marcar todas las notificaciones como leídas")
           public ResponseEntity<Void> markAllAsRead() {
               UserPrincipal principal = authService.getCurrentPrincipal();
               notificationService.markAllAsRead(principal.getId());
               return ResponseEntity.noContent().build();
           }
       }
       ```

  4. **Capa de Negocio (Backend Service):**
     - **Clase Service:** [NotificationService](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/domain/service/NotificationService.java)
     - **Métodos de implementación:** `sendNotification`, `getNotificationsForStudent`, `getUnreadCount`, `markAsRead`, `markAllAsRead`.
     - **DTO de mapeo:** [NotificationDTO](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/domain/dto/NotificationDTO.java)
     - **Validaciones:** Se obtiene el identificador de usuario a través del token JWT para restringir que un alumno pueda consultar o editar las notificaciones de terceros. Valida la existencia de la notificación por ID en la base de datos al marcarla de manera individual.
     - **Snippet de código Java:**
       ```java
       public void sendNotification(User student, String message) {
           Notification notification = Notification.builder()
                   .student(student)
                   .message(message)
                   .createdAt(LocalDateTime.now())
                   .read(false)
                   .build();
           notificationRepository.save(notification);
       }

       @Transactional(readOnly = true)
       public List<NotificationDTO> getNotificationsForStudent(Long studentId) {
           return notificationRepository.findByStudentIdOrderByCreatedAtDesc(studentId).stream()
                   .map(notificationMapper::toDto)
                   .toList();
       }

       @Transactional(readOnly = true)
       public long getUnreadCount(Long studentId) {
           return notificationRepository.countByStudentIdAndReadFalse(studentId);
       }

       public void markAsRead(Long notificationId) {
           Notification notification = notificationRepository.findById(notificationId)
                   .orElseThrow(() -> new ResourceNotFoundException("Notificación no encontrada con id: " + notificationId));
           notification.setRead(true);
           notificationRepository.save(notification);
       }

       public void markAllAsRead(Long studentId) {
           List<Notification> unread = notificationRepository.findByStudentIdOrderByCreatedAtDesc(studentId).stream()
                   .filter(n -> !n.isRead())
                   .toList();
           for (Notification n : unread) {
               n.setRead(true);
           }
           notificationRepository.saveAll(unread);
       }
       ```

  5. **Capa de Persistencia (Repository & Entity):**
     - **Repository:** [NotificationRepository](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/persistance/repository/NotificationRepository.java)
     - **Spring Data JPA Methods:**
       - `List<Notification> findByStudentIdOrderByCreatedAtDesc(Long studentId);`
       - `long countByStudentIdAndReadFalse(Long studentId);`
     - **JPA Entity:** [Notification](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/persistance/entity/Notification.java) (Mapeado a la tabla `notifications`).
     - **Funcionamiento de Base de Datos:**
       La inserción de alertas realiza un comando SQL `INSERT INTO notifications(student_id, message, created_at, is_read) VALUES(?, ?, ?, ?)`. El marcar la lectura masiva o individual de alertas actualiza la columna lógica `is_read` mediante un `UPDATE notifications SET is_read = true WHERE id = ?`.

  6. **Camino de Retorno:**
     - El backend responde al cliente enviando la lista en formato JSON de objetos `NotificationDTO` o el conteo de pendientes, con código HTTP 200 OK (o HTTP 204 No Content para las solicitudes PUT de actualización de lectura).
     - En el frontend ([Dashboard.jsx](file:///d:/Workspace/utp/EduTrack/Frontend/src/components/Dashboard.jsx)), las variables de estado `notifications` y `unreadCount` del tray se actualizan con la respuesta obtenida. Esto gatilla que React actualice de forma reactiva la campana mostrando la insignia roja con el conteo de pendientes y la lista interna de notificaciones (resaltando con fondo más claro las alertas que todavía no han sido leídas por el estudiante).
