# RF 11: Cálculo automático de promedios

- **Explicación del Requerimiento:**
  El sistema EduTrack calcula automáticamente el promedio consolidado de calificaciones para cada estudiante matriculado en una sección. La evaluación académica sigue el modelo ponderado oficial de la UTP:
  - **Prácticas Calificadas (PC1, PC2, PC3):** Cada una tiene un peso del **20%** (total de 60%).
  - **Evaluación Permanente (PA):** Promedio aritmético de todas las tareas o evaluaciones del tipo "PA", con un peso del **10%**.
  - **Examen Final (EXFINAL):** Tiene un peso del **30%**.
  
  El cálculo del promedio consolidado parcial o final se ajusta de forma dinámica según las calificaciones que ya se encuentran registradas. La fórmula matemática para el promedio ponderado actual es:
  $$Promedio Ponderado = \frac{\sum (Nota_{i} \times Peso_{i})}{\sum Peso_{i}}$$
  El valor final es redondeado a dos cifras decimales.

- **Flujo de Código Paso a Paso:**

  1. **Interfaz de Usuario (Frontend):**
     - **Archivo de componente:** [Grades.jsx](file:///d:/Workspace/utp/EduTrack/Frontend/src/components/Grades.jsx)
     - **Acción del usuario:** El usuario selecciona un periodo académico y luego una sección de clases. La función `handleSectionChange` gatilla la carga de notas llamando a la API.
     - **Código exacto de fetch de Javascript:**
       En [api.js](file:///d:/Workspace/utp/EduTrack/Frontend/src/services/api.js):
       ```javascript
       grades: {
         record: (data) => request('/grades', {
           method: 'POST',
           body: JSON.stringify(data),
         }),
         getSectionGrades: (sectionId) => request(`/grades/section/${sectionId}`),
         getFinalGrades: (sectionId) => request(`/grades/section/${sectionId}/final`)
       }
       ```
       En [Grades.jsx](file:///d:/Workspace/utp/EduTrack/Frontend/src/components/Grades.jsx):
       ```javascript
       const finals = await api.grades.getFinalGrades(sectionId);
       setFinalGrades(user.role === 'STUDENT' ? finals.filter(fg => Number(fg.studentId) === Number(user.id) || fg.studentUsername.toLowerCase() === user.username.toLowerCase()) : finals);
       ```

  2. **Capa de Red:**
     - **HTTP Method:** `GET`
     - **Base URL:** `http://localhost:8081/api` (desarrollo) o `http://13.59.189.231/api` (producción)
     - **Path:** `/grades/section/{sectionId}/final`
     - **Request parameters:** `sectionId` en la ruta como `@PathVariable` de tipo `Long`.
     - **Request body format:** Vacío.
     - **Cabeceras obligatorias:** `Authorization: Bearer <token>` para validar autenticación y rol.

  3. **Controlador (Backend Controller):**
     - **Clase Controller:** [GradeController](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/web/controller/GradeController.java)
     - **Método del endpoint:** `getSectionFinalGrades`
     - **Snippet de código Java:**
       ```java
       @GetMapping("/section/{sectionId}/final")
       @PreAuthorize("hasAnyRole('STUDENT', 'TEACHER', 'ADMIN')")
       @Operation(summary = "Obtener el consolidado de notas (PC1, PC2, PC3, PA, EXFINAL) de una sección")
       public ResponseEntity<List<FinalGradeDTO>> getSectionFinalGrades(@PathVariable Long sectionId) {
           return ResponseEntity.ok(gradeService.getSectionFinalGrades(sectionId));
       }
       ```

  4. **Capa de Negocio (Backend Service):**
     - **Clase Service:** [GradeService](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/domain/service/GradeService.java)
     - **Método de implementación:** `getSectionFinalGrades`
     - **DTO de mapeo:** [FinalGradeDTO](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/domain/dto/FinalGradeDTO.java)
     - **Validaciones:** Filtra las notas de los alumnos matriculados y asocia cada una con la categoría correspondiente. Si el rol que realiza la consulta es de estudiante (`ROLE_STUDENT`), se restringe la lista para devolver únicamente su propio consolidado de notas.
     - **Snippet de código Java:**
       ```java
       @Transactional(readOnly = true)
       public List<FinalGradeDTO> getSectionFinalGrades(Long sectionId) {
           org.springframework.security.core.Authentication auth = org.springframework.security.core.context.SecurityContextHolder.getContext().getAuthentication();
           String currentUsername = auth != null ? auth.getName() : "";
           boolean isStudent = auth != null && auth.getAuthorities().stream()
                   .anyMatch(a -> a.getAuthority().equals("ROLE_STUDENT"));

           List<Enrollment> enrollments = enrollmentRepository.findBySectionId(sectionId);
           if (isStudent) {
               enrollments = enrollments.stream()
                       .filter(e -> e.getStudent().getUsername().equalsIgnoreCase(currentUsername))
                       .collect(Collectors.toList());
           }

           List<Assignment> assignments = assignmentRepository.findBySectionId(sectionId);
           List<Grade> sectionGrades = gradeRepository.findByAssignmentSectionId(sectionId);
           
           List<FinalGradeDTO> result = new ArrayList<>();

           for (Enrollment enr : enrollments) {
               FinalGradeDTO finalGrade = FinalGradeDTO.builder()
                       .studentId(enr.getStudent().getId())
                       .studentUsername(enr.getStudent().getUsername())
                       .build();

               // Filter student grades from pre-fetched section grades
               List<Grade> studentGrades = sectionGrades.stream()
                       .filter(g -> g.getStudent().getId().equals(enr.getStudent().getId()))
                       .collect(Collectors.toList());

               // Map assignments to grades (using a merge function to prevent duplicate key exception)
               Map<Long, Double> gradeMap = studentGrades.stream()
                       .collect(Collectors.toMap(g -> g.getAssignment().getId(), Grade::getScore, (g1, g2) -> g2));

               double sumPA = 0.0;
               int countPA = 0;

               for (Assignment a : assignments) {
                   Double score = gradeMap.get(a.getId());
                   if (score != null) {
                       if ("PC1".equals(a.getCategory())) finalGrade.setPc1(score);
                       if ("PC2".equals(a.getCategory())) finalGrade.setPc2(score);
                       if ("PC3".equals(a.getCategory())) finalGrade.setPc3(score);
                       if ("EXFINAL".equals(a.getCategory())) finalGrade.setExfinal(score);
                       if ("PA".equals(a.getCategory())) {
                           sumPA += score;
                           countPA++;
                       }
                   }
               }

               // Calculate PA Average
               if (countPA > 0) {
                   finalGrade.setPa(sumPA / countPA);
               }

               // Calculate Final Average: weighted only on components that are actually graded (current weighted average)
               Double pc1 = finalGrade.getPc1();
               Double pc2 = finalGrade.getPc2();
               Double pc3 = finalGrade.getPc3();
               Double pa = finalGrade.getPa();
               Double exfinal = finalGrade.getExfinal();

               double weightedSum = 0.0;
               double totalWeight = 0.0;

               if (pc1 != null) {
                   weightedSum += pc1 * 0.2;
                   totalWeight += 0.2;
               }
               if (pc2 != null) {
                   weightedSum += pc2 * 0.2;
                   totalWeight += 0.2;
               }
               if (pc3 != null) {
                   weightedSum += pc3 * 0.2;
                   totalWeight += 0.2;
               }
               if (pa != null) {
                   weightedSum += pa * 0.1;
                   totalWeight += 0.1;
               }
               if (exfinal != null) {
                   weightedSum += exfinal * 0.3;
                   totalWeight += 0.3;
               }

               if (totalWeight > 0.0) {
                   double finalAvg = weightedSum / totalWeight;
                   finalGrade.setFinalAverage(Math.round(finalAvg * 100.0) / 100.0);
               } else {
                   finalGrade.setFinalAverage(null);
               }

               result.add(finalGrade);
           }

           return result;
       }
       ```

  5. **Capa de Persistencia (Repository & Entity):**
     - **Repository:** 
       - [GradeRepository](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/persistance/repository/GradeRepository.java) -> `List<Grade> findByAssignmentSectionId(Long sectionId);`
       - [EnrollmentRepository](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/persistance/repository/EnrollmentRepository.java) -> `List<Enrollment> findBySectionId(Long sectionId);`
       - [AssignmentRepository](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/persistance/repository/AssignmentRepository.java) -> `List<Assignment> findBySectionId(Long sectionId);`
     - **JPA Entities:**
       - [Grade](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/persistance/entity/Grade.java) (Mapeado a la tabla `grades`)
       - [Enrollment](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/persistance/entity/Enrollment.java) (Mapeado a la tabla `enrollments`)
       - [Assignment](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/persistance/entity/Assignment.java) (Mapeado a la tabla `assignments`)
     - **Funcionamiento de Base de Datos:**
       La consulta recupera a todos los alumnos matriculados y sus notas de cada asignación dentro de la sección para construir el mapa dinámico en memoria. No hay almacenamiento directo del promedio consolidado en la tabla `grades`; se calcula dinámicamente al vuelo para evitar redundancia e inconsistencia ante actualizaciones.

  6. **Camino de Retorno:**
     - El `GradeService` retorna la lista de objetos `FinalGradeDTO`.
     - El `GradeController` los serializa como un array JSON de objetos y los envía de vuelta al cliente con código HTTP 200 OK.
     - En el frontend, el hook de la sección recibe la respuesta exitosa en el componente `Grades.jsx`, actualiza el estado `finalGrades` por medio de `setFinalGrades`, lo que dispara un nuevo renderizado del DOM que actualiza de inmediato el panel consolidado y el promedio ponderado semestral en la tabla.
