# RF 07: Entrega de evaluaciones

- **Explicación del Requerimiento**:
  Este requerimiento permite a los estudiantes matriculados en una sección subir un archivo digital como entrega (evidencia de desarrollo) de una evaluación antes de la fecha límite establecida. El sistema valida que el plazo no haya vencido y que el número de entregas realizadas no supere el límite máximo de intentos permitidos (`maxAttempts`) configurado para la evaluación. Al cumplir las condiciones, el archivo se guarda localmente en el servidor de archivos y se crea el correspondiente registro de entrega.

- **Flujo de Código Paso a Paso**:

  1. **Interfaz de Usuario (Frontend)**:
     - **Componente React**: [Assignments.jsx](file:///d:/Workspace/utp/EduTrack/Frontend/src/components/Assignments.jsx)
     - **Acción del Usuario**: El estudiante selecciona un archivo local de su dispositivo mediante un input de archivo al lado de la evaluación correspondiente y presiona el botón "Enviar". Esto dispara la función `handleFileUpload`.
     - **Código de JavaScript (Fetch)**:
       ```javascript
       // In d:/Workspace/utp/EduTrack/Frontend/src/components/Assignments.jsx
       const handleFileUpload = async (assignmentId) => {
         if (!file) return alert('Selecciona un archivo primero');
         try {
           // Invoca el endpoint enviando el id de evaluación, id de estudiante y el archivo
           await api.assignments.submit(assignmentId, user.id, file);
           alert('Entrega enviada con éxito!');
           setFile(null);
         } catch (err) {
           alert('Error al enviar: ' + err.message);
         }
       };

       // In d:/Workspace/utp/EduTrack/Frontend/src/services/api.js
       assignments: {
         submit: (assignmentId, studentId, comment, file) => {
           // Si se invoca con 3 parámetros como en Assignments.jsx (comment es undefined o se mapea al archivo):
           // En este caso, el file se pasa y se crea un objeto FormData
           const formData = new FormData();
           formData.append('studentId', studentId);
           if (comment && typeof comment === 'string') {
             formData.append('comment', comment);
           }
           // En caso de que se pase el archivo como tercer parámetro, se maneja el caso
           const finalFile = file || comment; 
           formData.append('file', finalFile);
           
           return request(`/assignments/${assignmentId}/submit`, {
             method: 'POST',
             body: formData,
           });
         },
         // ...
       }
       ```

  2. **Capa de Red**:
     - **Método HTTP**: `POST`
     - **Base URL**: `http://localhost:8081/api`
     - **Ruta (Path)**: `/assignments/{assignmentId}/submit`
     - **Parámetros de Solicitud**: 
       - `studentId` (Query Param / Form Data param: `Long`)
       - `comment` (Opcional, Form Data param: `String`)
       - `file` (Multipart file: `Binary`)
     - **Cuerpo de Solicitud (Request Body)**: Codificado como `multipart/form-data`.

  3. **Controlador (Backend Controller)**:
     - **Nombre de Clase**: [AssignmentController.java](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/web/controller/AssignmentController.java)
     - **Método del Endpoint**: `submitAssignment`
     - **Snippet de Código Java**:
       ```java
       @PostMapping("/{assignmentId}/submit")
       @PreAuthorize("hasRole('STUDENT')")
       @Operation(summary = "Entregar evaluación (RF07)", description = "Permite a un estudiante subir un archivo local como entrega. Accesible solo por ESTUDIANTE.")
       public ResponseEntity<AssignmentSubmissionDTO> submitAssignment(
               @PathVariable Long assignmentId,
               @RequestParam("studentId") Long studentId,
               @RequestParam(value = "comment", required = false) String comment,
               @RequestParam("file") MultipartFile file) {
           return new ResponseEntity<>(assignmentService.submitAssignment(assignmentId, studentId, comment, file), HttpStatus.CREATED);
       }
       ```

  4. **Capa de Negocio (Backend Service)**:
     - **Nombre de Clase**: [AssignmentService.java](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/domain/service/AssignmentService.java)
     - **Implementación del Método**: `submitAssignment`
     - **Mapeo de DTO**: Convierte la entidad creada `AssignmentSubmission` a `AssignmentSubmissionDTO` mediante builder, incluyendo la ruta normalizada.
     - **Validaciones**:
       - Valida que la evaluación exista en BD.
       - Valida que el estudiante exista en BD.
       - Valida si la fecha actual es posterior a la fecha límite (`assignment.getEndDate()`), arrojando `BusinessException("El plazo para esta tarea ha expirado.")`.
       - Cuenta las entregas previas con `submissionRepository.countByAssignmentIdAndStudentId(assignmentId, studentId)` y compara con `maxAttempts`. Si lo iguala o supera, arroja `BusinessException("Has alcanzado el límite de intentos permitidos...")`.
     - **Snippet de Código Java**:
       ```java
       @Transactional
       public AssignmentSubmissionDTO submitAssignment(Long assignmentId, Long studentId, String comment, MultipartFile file) {
           Assignment assignment = assignmentRepository.findById(assignmentId)
                   .orElseThrow(() -> new ResourceNotFoundException("Assignment not found"));
           User student = userRepository.findById(studentId)
                   .orElseThrow(() -> new ResourceNotFoundException("Student not found"));

           if (LocalDateTime.now().isAfter(assignment.getEndDate())) {
               throw new BusinessException("El plazo para esta tarea ha expirado.");
           }

           // Validate submission attempts limit
           int maxAttempts = assignment.getMaxAttempts() != null ? assignment.getMaxAttempts() : 1;
           int currentAttempts = submissionRepository.countByAssignmentIdAndStudentId(assignmentId, studentId);
           if (currentAttempts >= maxAttempts) {
               throw new BusinessException("Has alcanzado el límite de intentos permitidos para esta evaluación (" + maxAttempts + ").");
           }

           String filePath = fileStorageService.storeFile(file);

           AssignmentSubmission submission = AssignmentSubmission.builder()
                   .assignment(assignment)
                   .student(student)
                   .submissionDate(LocalDateTime.now())
                   .filePath(filePath)
                   .studentComment(comment)
                   .build();

           AssignmentSubmission saved = submissionRepository.save(submission);

           return AssignmentSubmissionDTO.builder()
                   .id(saved.getId())
                   .assignmentId(saved.getAssignment().getId())
                   .studentId(saved.getStudent().getId())
                   .submissionDate(saved.getSubmissionDate())
                   .filePath(normalizeFilePath(saved.getFilePath()))
                   .studentComment(saved.getStudentComment())
                   .build();
       }
       ```

  5. **Capa de Persistencia (Repository & Entity)**:
     - **Repositorio**: [AssignmentSubmissionRepository.java](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/persistance/repository/AssignmentSubmissionRepository.java)
       - **Métodos JPA Utilizados**: `countByAssignmentIdAndStudentId(Long assignmentId, Long studentId)` para contar los intentos previos del estudiante y `save(...)` para insertar el registro de entrega.
     - **Entidad**: [AssignmentSubmission.java](file:///d:/Workspace/utp/EduTrack/Backend/src/main/java/com/utp/EduTrack/persistance/entity/AssignmentSubmission.java)
       - **Tabla de Base de Datos**: `assignment_submissions`
       - **Detalles de Registro/Actualización/Consulta**: Se realiza un `INSERT` en la tabla `assignment_submissions` asociando las llaves foráneas `assignment_id` y `student_id` junto con los campos `submission_date` (fecha y hora local actual), `file_path` (directorio del servidor de archivos donde se almacenó el archivo) y `student_comment`.

  6. **Camino de Retorno**:
     - El backend guarda físicamente el archivo en disco, persiste el registro en la base de datos y retorna un HTTP `201 Created` junto con el JSON del DTO de la entrega.
     - El frontend recibe la confirmación. En `Assignments.jsx`, la función `handleFileUpload` captura la resolución exitosa de la promesa de Fetch y muestra `alert('Entrega enviada con éxito!')`, reestableciendo el estado del archivo local a `null` (`setFile(null)`).
     - Si el docente visualiza la sección de entregas en `Grades.jsx` o `CourseContent.jsx`, al re-renderizar o consultar, los datos de la entrega del alumno (como la opción de previsualizar y el archivo enviado) aparecen de forma inmediata en la interfaz gracias a la actualización de la lista de entregas `submissions` mediante `setSubmissions(subs)`.
